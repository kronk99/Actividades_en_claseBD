#include <iostream>
using namespace std;
template <class T>
class mina{
    private:
        List<T> *mina;
        int numPuertas
    public:
        mina(int number){
            mina = new List<T>;
            numPuertas = number;
            //aca va un ciclo for para construir una lista de las puertas iniciales
            //con el random de numero , esa funcion debe restarle al int counter
            mina->add() aca se añade la lista con las puertas iniciales
        }
        void insertPuertas(){
            //funcion que va a crear listas y se las inserta a cada una de las puertas
            // va rotando para que la creacion sea uniforme.
        }


};